const server = "http://localhost:8080";
const uploadFileEndpoint = server + "/api/files/upload";
const getFileEndpoint = server + "/api/files/download";

export default {
    server,
    uploadFileEndpoint,
    getFileEndpoint
}